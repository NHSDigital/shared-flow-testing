[pytest]
addopts = --strict-markers -v
junit_family=xunit1
markers =
    errors: checks against error conditions
    happy_path: checks happy path scenarios
    splunk: splunk logging tests
    debug: add this for debugging individual test
